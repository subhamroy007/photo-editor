import { AppHeader } from "../../components/utility/AppHeader";
import { Screen } from "../../components/utility/Screen";

export function Favourites() {
  return (
    <Screen>
      <AppHeader leftIcon="arrow-left" title={"Favourites"} />
    </Screen>
  );
}
